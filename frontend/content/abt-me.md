---
title:
  en: abt-me
  zh: 关于我
---

<!-- en -->
Placeholder. Replace this with something worth reading.

The page is `content/abt-me.md` — plain markdown, same format as any article,
so [tooltip content="悬浮或点一下就能看到这种注释。"]the markup[/tooltip] and the
language split work here too. Frontmatter takes `fontScale` if this ends up
wanting a different size from the articles.

<!-- zh -->
占位。回头换成值得一读的东西。

这一页是 `content/abt-me.md`，就是普通的 markdown，格式和文章完全一样，
所以[tooltip content="悬浮或点一下就能看到这种注释。"]正文标记[/tooltip]和中英分栏
在这里照样能用。想让字号和文章不一样的话，frontmatter 里加 `fontScale` 就行。
